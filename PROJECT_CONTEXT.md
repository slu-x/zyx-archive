# 项目背景说明 · Archive of Lay（原"张艺兴考古网站"）Phase 0

> 写给接手这个项目的 AI（Claude / Codex 等）看的背景文档。用户完全没有编程经验，解释和操作都要照顾到这一点。服务器登录等运维细节不放在这份公开仓库文档里，如果需要，向用户本人询问。
>
> 最后更新：2026-09-22（大批量内容导入 + honor/caption 新字段 + 品牌活动与杂志分类重组 + 分类卡片预览逻辑修复之后）

## 一、项目是什么

长期目标：把张艺兴过去约十四年的公开资料（微博、抖音、小红书、知乎、贴吧、B站、YouTube、新闻媒体等）整理成一个"数字考古档案馆"。核心组织逻辑是 时间 -> 事件/作品 -> 相关物料，而不是按平台分类——目的是让粉丝能从一个舞台/事件出发，顺藤摸瓜看到前因后果的全部相关资料。

当前阶段：Phase 0，纯技术验证版，不追求好看、不追求资料齐全。Phase 0 要回答的问题：
1. 能否方便地增删改查跨平台链接资料
2. 用户点击链接能否顺利跳转到对应平台
3. 能否免费/低成本部署一个测试网站
4. 中国大陆用户能否顺畅访问
5. 手机端 + 不同浏览器（尤其微信内置浏览器）的跨平台跳转体验如何
6. 以后能否不重写整个网站就迁移到别的服务器

用户反复强调的原则：不要过度设计。不要提前引入数据库、后台管理系统、用户账号系统、构建工具链等"以后可能需要"的东西。

## 二、技术架构（已定型，非必要不要改动）

- 纯静态网站：HTML + CSS + 原生 JavaScript，无框架，无构建步骤
- 数据格式：每个"事件"是 data/events/ 目录下一个独立 JSON 文件；data/events/manifest.json 是文件名清单；首页显示顺序由每个事件的 date 字段自动排序
- 为什么每个事件一个独立文件：方便非程序员或 AI 单独编辑某一个事件而不影响其他事件
- 为什么不做管理后台/CMS：目前长期只有用户一个人改内容。日常维护方式是用户把资料发给 AI，AI 直接改 JSON 文件并提交 GitHub。不要主动建议做在线可视化编辑后台，这是权衡过的决定（安全成本 + 维护成本不划算），除非以后编辑人数明显变多

事件 JSON 格式：
```json
{
  "id": "2025-08-01-test-stage-a",
  "title": "事件标题",
  "date": "2025-08-01",
  "summary": "一两句简介",
  "materials": [
    { "category": "正式内容", "platform": "微博", "title": "链接标题", "url": "https://..." }
  ]
}
```
- category 常用值：正式内容 / 官方物料 / 当时的讨论 / 其他
- platform 有专属颜色的值（定义在 js/common.js）：微博 抖音 小红书 知乎 贴吧 Bilibili/B站 YouTube 媒体，其他名称会用默认灰色显示

### 多层分类导航（2026-08-30 新增，架构升级）

原本首页只是"按日期排序的事件列表"。后来用户提出想要更接近粉丝实际浏览习惯的**多层分类**结构（比如"综合 > 舞台之神 > 霸王别姬"这种，而不是单纯按时间），于是加了一层"目录树"：

- `data/nav.json`：定义整个导航树。顶层 `tabs` 数组是首页看到的几个大分类（目前是：综合/跨越平台/昵称由来/喜糖英语角/考古汇总），每个 tab 可以有 `color`（该大类的主题色，浅紫色系，取自用户指定的两组小红书配色图）、`intro`（可选，会显示在该分类页面顶部的引言文字，比如"莲乎"下面的那句堂吉诃德语录）、`children`（子节点数组，可以继续嵌套分类，也可以是叶子内容）
- 叶子内容有两种：`{"type": "event", "eventId": "..."}` 引用一个 `data/events/*.json` 事件文件（复用原有的事件详情页展示逻辑）；`{"type": "link", "platform": "...", "title": "...", "url": "..."}` 直接是一条外部链接
- `js/nav.js`：读取 nav.json、按 id 查找节点并生成面包屑路径的公共逻辑
- `js/category.js`：渲染任意一层分类页面（分类卡片 / 事件卡片 / 链接卡片），`index.html`（无 id，代表根目录）和 `category.html?id=xxx`（任意一层）共用同一套渲染逻辑
- 原来的事件详情页 `event.html` / `js/event.js` 基本没变，还是展示某一个事件下按 category 分组的所有物料链接
- 视觉上换成了深紫色背景（`#312a44`）+ 各大 tab 各自的浅紫色卡片，配色值都在 `css/style.css` 顶部的 `:root` 变量里，改配色只需要改那几行

**目前 nav.json 里大部分分类还是空的占位符**（比如"昵称由来"下面的锅盖/莲咪/宝宝/bob，"考古汇总"下面几个微博账号），只有"综合 › 舞台之神"下面放了霸王别姬这一个真实事件，"跨越平台 › 莲乎"下面放了一条真实知乎链接。以后往里填内容，直接编辑 `data/nav.json` 对应节点的 `children` 数组即可，不需要改 HTML/JS。

### 目录树 2026-09-06 大更新：叶子内容加了两种新类型

用户提供了一份更新过的腾讯文档（后来发现在线抓取不到实时内容，改用导出 docx / pdf 的方式读取——docx 能读出文字和红色字体标记，但部分超链接在 docx 里丢失了实际文字/只剩下裸链接，后来发现导出成 PDF 反而能通过 PDF 的链接注释（annotation）拿到"贝壳英语角"那批文章的真实链接，混合使用两种格式互相印证）。这次批量导入把 nav.json 从"5 个大 tab、2 条真实内容"扩充成了"6 个大 tab、约 90 条链接"，并且给 leaf 内容 schema 新增了两种类型（`js/category.js` 里的 `renderChildCard` 函数处理）：

- `{"type":"divider","label":"2017年"}`：纯展示用的分界线，不生成新页面，用在"同一个分类下面想按年份/阶段分组，但不想拆成好几个 tab"的场景（用户原话："有一些有年份就画个分界线写XX，再继续加链接，不把它作为单独板块了"）。样式是 `.year-divider`，一条横线中间夹着年份文字
- `{"type":"link","platform":"...","title":"...","url":""}`：`url` 留空字符串表示"这条先占位，链接还没给"，渲染成灰色、不可点击、带"待补充"标签的样式（`.link-card-pending` / `.pending-tag`），不会报错也不会显示成死链接

同时确认了"霸王别姬"这个事件的真实日期是 **2019-12-31**（2019年跨年舞台），已更新 `date` 字段，并在 nav.json 里正式挂到"综合›舞台之神›跨年舞台"下面，和其他几年的跨年舞台链接（141231/151231/161231/171231-占位/181231）排在一起（这个事件文件后来又改了名，见下面 2026-09-07 的更新）。

顶层 tab 也跟着这次更新变成了 6 个（原来是 5 个）：综合、跨越平台、**互相陪伴的日子**（原来的"昵称由来"降级成了它下面的一个子分类）、**贝壳英语角**（原来叫"喜糖英语角"，改名了）、**公益**（全新，目前是空的）、考古汇总。

### "综合"大改版 + PDF 里"绿色 Claude 指令"约定（2026-09-07）

用户又发来一版更新的 PDF，这次除了红色字体（＝忽略的备注）以外，**还出现了绿色字体，专门写"Claude指令"**（比如"合集单独放一个tab，其余每年作为event，标题为年份+舞台名"）。识别方法：用 `pdfplumber` 读每个字符的 `non_stroking_color`（RGB 0-1 浮点数），红色判断 `r>0.5 且 r-g>0.2 且 r-b>0.2`，绿色判断 `g>0.15 且 g-r>0.08 且 g-b>0.05 且 r<0.3`（实测颜色值是 `(1,0,0)` 红、`(0.094,0.302,0.196)` 绿）。**以后如果用户的文档里还有绿色字体，大概率也是类似的"结构性指令"，要认真解析，不能像红色文字一样直接忽略。**

按这几条绿色指令，把"综合"下面大改了一遍：
- **跨年舞台**：合集链接单独做成一个子分类，其余每一年（141231～251231）都做成一个独立"事件"（`type:"event"`），191231 当晚其实有两个舞台（《霸王别姬》+《梦不落雨林》），拆成了两个事件
- **春晚**：跟跨年舞台同样处理（合集单独一个子分类 + 每年一个事件），并且央视春晚和地方台春晚**合并按年份从新到旧排**，不再单独区分
- **演唱会**：目前只有"一巡"下面列了几个城市站（吉隆坡/新加坡/曼谷/旧金山/长沙线上），每个城市站也建成了一个事件（暂时都是空的，等以后补资料）；二巡/三巡/四巡/五巡/2.5巡/跨年演唱会都还是空分类
- **其他舞台**：改成"按年份画分界线（divider）+ 每个舞台一个完整事件"，目前只有 2019 年有内容，且原本 2 个舞台的资料后来又按用户要求**合并成了 1 个事件**（`other-stage-2019`）

顺带确认了一个之前悬而未决的疑问：霸王别姬"官方完整舞台"和"观察日记"曾经是同一个 B 站 BV 号，怀疑是复制粘贴出错——**这次证实不是错误**，是同一个多集视频的不同分集（用网址里的 `&p=` 参数区分），"官方舞台"其实应该是微博链接，"观察日记"是 B 站 `&p=2`，另外还补了两条"饭拍"（`&p=1` 默认集 + 另一个 BV 号）。

事件文件也顺手做了一次清理：删掉了最早 Phase 0 时代占位用的 `2025-08-01-test-stage-a.json`（霸王别姬现在用更有意义的文件名 `new-year-2019-farewell-concubine.json`），`data/events/manifest.json` 里对应的死引用也删掉了（这个 manifest 文件是旧版"平铺事件列表"用的，多层导航上线后已经不是必需品，但还留着没删，`js/common.js` 的 `loadAllEvents()` 还在用它，只是没有任何页面调用这个函数了）。

还发现并修了两个和"数据量变大"有关的技术问题：
1. `js/search.js` 建索引时，原本是**一个个排队去请求**每个事件文件（`for` 循环里 `await`），事件数量一多会让"第一次点搜索框"明显变慢；改成了**先同步走完整棵树收集所有事件引用，再用 `Promise.all` 一次性并发请求**，不管事件数量多少，建索引时间基本不变
2. `js/common.js` 的 `fetchJson()` 原本用默认的 `fetch(url)`，会被浏览器的 HTTP 缓存"卡住"——**改成 `fetch(url, {cache:"no-cache"})`**，保证访问者不用手动刷新也能看到你更新后的最新内容（这个问题是本地反复调试这台服务器几十次之后才在浏览器缓存里暴露出来的，真实访客第一次访问基本不会遇到，但作为长期维护的网站还是修一下更稳妥）

### 网站内容备份 Word 文档 + 生成工具（2026-09-07 新增）

用户希望有一份跟网站实际内容同步、方便他自己编辑的 Word 文档（而不是继续维护一份容易跟网站脱节的腾讯文档）。做法：写了一个脚本 `tools/generate-content-doc.js`（用 `docx` 这个 npm 包），读取 `data/nav.json` + 所有 `data/events/*.json`，自动生成一份编号大纲格式的 Word 文档（`Archive of Lay - 网站内容备份.docx`，放在仓库根目录，但这个 .docx 本身已经加进 `.gitignore`，不会提交到 GitHub——因为它是"可以随时重新生成的衍生文件"，`data/nav.json` 才是真正的数据源）：

- 每一层分类/每个事件都用了 Word 的**真正标题样式**（Heading1~Heading6，按嵌套深度对应），这样用户在 Word 里开"导航窗格"能直接跳转，也能一键生成目录
- 每条物料链接都是可点击的完整网址（`ExternalHyperlink`）
- 绿色加粗字体（`js` 脚本里 `pushNote()` 函数）＝给以后维护者/AI 看的结构性说明（比如"跨年舞台每一年是独立事件""191231 拆成了两个事件"这类），跟用户自己文档里"绿色=Claude指令"的约定是同一套颜色语言，只是这次反过来是我（AI）留给以后的人看
- 运行方式写在脚本最上面的注释里：`cd tools && npm install docx && node generate-content-doc.js`（`npm install` 只需要跑一次，这台机器上 docx 包不在全局/预装位置，需要在 `tools/` 目录本地装一次）
- 以后的工作流可以是：用户直接在这份 Word 文档上编辑（加/改/删标题和链接），改完导出成 Word 或 PDF 发给 AI，AI 对照更新 `data/nav.json`/`data/events/`，而不需要再单独维护一份腾讯文档

### 资料收集表 Excel + 腾讯文档协作工作流（2026-09-11 新增，取代了"用户改 Word 文档"的设想）

Word 文档备份工具用了一轮之后，用户反馈"每次导出 PDF/Word 让 AI 整篇重新读、猜哪里改了"效率太低，也没法让多个粉丝一起贡献链接。改成了：`tools/generate-collection-sheet.py` 读 `data/nav.json` + 所有事件文件，生成一份 `Archive of Lay - 资料收集表.xlsx`（每个大 tab 一个 sheet，固定列：分类路径/标题/链接/平台/年份/附注/是否文件夹链接/状态）。管理员把它传到腾讯文档分享给粉丝直接填，"分类路径"限制成下拉菜单（只能选现有分类，避免分类名字打歧义），"是否文件夹链接"选"是"代表这是一整个 B 站收藏夹/合集，AI 导入时会展开成一条条真实链接并自动去重，"状态"列让 AI 以后只处理没标"已导入"的新行，不用每次通读全部内容。**这条结论推翻了本文档之前"不建议做批量导入工具"的说法**，见下面第五节第 9 条。

### 视觉/交互小改动汇总（2026-09-11 ~ 09-15）

- 字体：自定义字体"白无常可可体"试过又撤了，**现在全站中文字体是系统自带的楷体**
- 首页大 banner 副标题现在是"以爱存档，赴岁岁年年"
- 卡片下面第二行的"子分类名字预览"效果，原来只有首页大 tab 才有，**现在只要某个分类下面有至少一个子分类（不限层级）就会显示**，混进去的事件按年份、链接按标题也会拼进预览里（细节见 README.md 5.10 小节）
- 原来浅灰色圆角的 `.node-tag` 小标签，**去掉了背景和圆角，改成纯白色文字**
- 2026-09-11 根据用户提供的新 PDF 做了一轮内容大更新（百亿影帝拆成电影/电视剧两个子分类并标注角色和年份等），具体清单见对应几次 git commit message

### 全站搜索（2026-08-30 新增）

`js/search.js`：完全在浏览器里跑的客户端搜索，不需要后端。逻辑是打开任意页面时，把 nav.json 全部节点 + 引用到的每个事件文件 + 事件里的每条物料都拉一遍，拼成一份内存里的搜索清单，输入框打字时做子字符串匹配，点结果直接跳转（分类/事件走站内链接，物料/链接走外部真实网址）。三个页面（index.html / category.html / event.html）的 header 里都加了搜索框，共用同一份逻辑。数据量不大的时候（现在这个阶段）完全够用，不需要额外的搜索引擎/索引服务。

### 品牌重命名为 "Archive of Lay"（2026-08-30 新增，紧接在买下 archiveoflay.com 域名之后）

用户注册了 `archiveoflay.com` 这个域名后，把网站的对外展示名称也同步改成了英文的 **"Archive of Lay"**（Lay 是张艺兴的英文艺名）：

- 三个页面的 `<title>` 标签、`<h1>` 都改成了 "Archive of Lay"，首页副标题改成"张艺兴档案馆 · 跨平台考古导航"
- `<meta name="apple-mobile-web-app-title">` 也同步改了，影响的是"添加到主屏幕"时 iPhone 上显示的名字
- 首页页脚文字改成了"张艺兴喜糖翻译博 · XingTweet"（这是资料/翻译来源的署名，不是网站开发者信息）
- 新增了 `assets/` 文件夹，放了三张网站图标图片：`favicon.png`（浏览器标签页图标）、`apple-touch-icon.png`（iPhone 添加到主屏幕的图标）、`icon-512.png`（大尺寸备用图标）。三个 HTML 页面的 `<head>` 里都加了对应的 `<link>` 标签引用这些图标。图标做过一次"居中"修复（`26bd0c1` 那次提交），确保图标是"完整 logo（圆形+文字）"再补边距垫成正方形，不是被裁切的样子
- **仓库内部代码变量、文件名、`data/` 里的内容都还是中文/原来的样子没有改**，这次改动纯粹是"网站展示给访客看的文字和图标"，不影响数据结构或架构

### 点赞（kudos）功能（2026-09-20 新增，网站第一次不再是纯静态）

给每条链接/每个分类板块/每个事件加了一个可点赞的紫色爱心（未点=描边空心，点了=实心），全站共享的真实点赞数。这需要一个能持久化写入的地方，纯静态做不到，所以给 VPS 加了一个极小的后端服务（`tools/kudos-server.py`，纯 Python 标准库 `http.server` + `sqlite3`，不依赖任何第三方包），只监听 VPS 本机的 127.0.0.1，通过 Nginx 反向代理 `/api/kudos` 对外提供服务，systemd 管理进程。前端新增 `js/kudos.js`。刻意没有用 Cloudflare Workers + KV/D1 来做这个——因为早就验证过 `*.workers.dev` 域名大陆访问不稳定，点赞这种高频交互挂在上面体验会差，VPS 已验证大陆连接良好，更合适。这个功能的数据（点赞数）完全独立存放在 VPS 上，不经过 GitHub、不写回 `nav.json`，设计上做到了可以随时完整移除、不影响网站其他部分（细节见本地专属的 PROJECT_HANDOFF.md）。

### 点赞汇总的性能优化与 events-bundle.json（2026-09-20~21）

点赞功能刚上线时，"往上汇总"这一步是纯前端实时算的：每次要显示一个分类/tab 的汇总数字，就要把它底下所有事件的 json 文件都现抓一遍。数据量还小时没问题，但首页这种要同时给好几个大 tab 算汇总的页面，一次性会多发出 30 多个请求，明显变慢——用户反馈"（尤其国内访问）还是有点慢，如果一百个粉丝同时用会不会更慢"。

第一次尝试的修法是"偷懒"：汇总时不再往下算进事件内部的物料链接，只加事件/文件夹自己的点赞数。请求数是降下来了，但**用户明确拒绝了这个妥协**（原话大意：还是希望某一条物料链接点赞，能往上算进它所在事件/分类的汇总数字）——功能深度不能因为性能让步，需要一个更好的解法。

真正的修法：新增 `tools/generate-events-bundle.py`，把 `data/events/` 下所有事件文件汇总成一个 `data/events-bundle.json`（`{eventId: 完整事件对象}` 的一个大字典）。前端只需要请求这一个文件一次（`js/common.js` 的 `fetchEventsBundle()`，请求结果会缓存，同一页面多处调用也只发一次网络请求），`js/kudos.js` 的 `computeKudosTotal()` 从这份缓存里查事件物料，不再逐个请求事件文件；`js/search.js` 建索引原本也是对每个事件文件单独发请求（虽然已经是 `Promise.all` 并发而非排队），现在同样改成读这个共享的 bundle，两个功能共用同一份缓存。效果：请求数量回到了引入点赞功能之前的基线（10 个左右，包含这一个 bundle 文件），点赞汇总深度完全保留——物料 → 事件 → 所在分类 → 一路到顶层 tab，一层都不少。

**⚠️ 这是一个衍生/生成文件，不是数据源，需要人工记得同步——这一点非常重要，已经踩过一次真实的坑：**

`data/events-bundle.json` 需要在**每次 `data/events/` 目录下的文件有任何新增、修改、删除之后**，手动重新运行 `python tools/generate-events-bundle.py` 生成，并把新生成的文件一起提交。忘记这一步不会报错、不会崩溃——只会让点赞汇总和搜索结果悄悄"过时"（读到旧内容，或者读不到新增的事件）。

2026-09-21 真实发生过一次：公益分类重构（见下一节）新增了 5 个分组事件（把好几条连续的赈灾物资微博打包成一个事件，跟"跨年舞台"的做法一样），后续又给"抗疫支援捐赠"这个事件追加了新物料链接，但两次改动之后都没有重新生成 bundle。结果是用户在"抗疫支援捐赠"事件详情页给一条物料点赞，物料自己那颗心显示点上了、数字也变了，但这次点赞**没有**如预期一样累加进事件卡片和"医疗健康"分类卡片显示的汇总数字里——因为它们读到的 bundle 里这个事件还是"0 条物料"的旧快照。用户通过网站截图报告发现问题，排查后确认是 bundle 没重新生成，跑一遍生成脚本、提交、推送后解决（commit `d9c7f70`）。

**结论：以后任何人（包括 AI）只要碰了 `data/events/*.json`，收尾时一定要习惯性地问自己"我重新生成 events-bundle.json 了吗"，这和改完 nav.json 要检查逗号，是这个项目里同一等级的"容易忘"清单项，README.md 第十节也写了同样的提醒给用户自己看。**

同一批改动里，也给点赞后端的 SQLite 加了 `PRAGMA journal_mode=WAL` + `PRAGMA busy_timeout=5000`（并发写入更安全，避免多个人同时点赞时互相锁住报错），并在服务器 Nginx 层面确认启用了 HTTP/2 和 gzip 压缩（之前 gzip 配置里实际压缩的文件类型那一行被注释掉了，等于名义上开了 gzip 但没有真正压缩任何 JS/CSS/JSON，一并修正）——这两项都是纯粹的性能/稳定性优化，不影响任何现有功能或数据结构。

### 公益（charity）分类重构 + 事件日期核实（2026-09-20~21）

用户提供了一份"资料收集表"里的公益 sheet，要求把原本平铺、没有二级分类的"公益" tab，按内容性质拆成 7 个子分类：救灾应急、医疗健康、儿童关怀、音乐/乡村教育、粉丝公益/倡导、体育/文化、其他，每个子分类内部按年份从新到旧排序。

处理方式：
- 同一场赈灾行动里连续发的好几条微博（比如"驰援广西洪灾"分好几批物资装车/发放，各发一条微博），参照"跨年舞台"/"其他舞台"已有的做法，合并打包成一个 `data/events/*.json` 完整事件（而不是好几条并列的独立链接）。目前有 5 个这样的分组事件：广西洪灾、辽宁洪灾、华北洪灾、西藏地震、抗疫支援捐赠（这一个是老事件，这次重新扩充了范围和物料，并改了标题）
- 没有年份信息的原始条目，逐条打开真实链接核实发布年份，不靠猜。微博链接优先用 `m.weibo.cn/status/<id>` 而不是桌面版 `weibo.com`——桌面版对未登录访问者会跳转到登录墙，`m.weibo.cn` 大多数情况不需要登录就能看到发布时间；如果遇到"新浪访客系统"验证页，等几秒后重新访问一次通常就能看到正文。**有个容易踩的坑**：微博对"当年"发布的帖子只显示"月-日"（比如"7-7"），对非当年发布的帖子才会显示完整年份（比如"2025-1-8"）——凭这条规则核实后发现，广西/辽宁这两个事件原本按"2025年"归档，实际发布年份其实是 2026 年，重新核实后两者的相对排序也跟着变了
- 核实/改完之后用一次性脚本（读 `nav.json` 的 charity 子树 + 对应事件文件）生成一份 `Archive of Lay - 公益分类结果.xlsx` 发给用户核对（这份 xlsx 是给用户看的快照，不是数据源，没有提交进仓库，跟"资料收集表"工作流是同一个原则——真正的数据永远是 `data/nav.json` + `data/events/`）
- 每个子分类内部也加上了和"其他舞台"一样的年份分界线（`{"type":"divider","label":"2025年"}`），唯一一条查不到年份的（染色体公益基金知乎链接）分界线标"年份不详"

### 大批量内容导入第二轮 + 两种新叶子类型：honor / caption（2026-09-21~22）

用户发来新一版"资料收集表"（`20260922- Archive of Lay - 资料收集表 (new).xlsx`），要求更新除"公益"以外的所有 sheet，并给了三条结构性规则：同一个子页面/子事件里出现的重复链接直接删掉；同一个页面超过 5 条链接就按时间倒序、加年份分界线；同一个事件超过 3 条链接就单独做成一个 `event` 而不是散落的 `link`。这一轮之后又追加了一次"历史遗留的未标注年份链接"专项排查——很多早期就分类好、但从来没标过年份的链接（莲乎、阿b小王子的妖党等），逐条打开真实网址核实发布年份再补分界线。

导入/核实规模：`data/nav.json` 目前是 **642 条 `link` + 39 个 `event` + 44 条 `honor` + 270+ 条 `divider`，139 个文件夹节点**（用一个一次性校验脚本确认过：所有 `event` 引用的文件都存在、没有重复 id、同一文件夹内没有重复链接）。

日期核实这次用到了几种可复用的技术手段（以后再遇到同类"这条链接是哪年发的"问题可以直接照搬）：
- **B 站**：`curl https://api.bilibili.com/x/web-interface/view?bvid=<BV>`（老式 `av` 号链接换成 `?aid=`），返回 JSON 里的 `data.pubdate`（Unix 时间戳）就是发布时间；Windows 上直接用 Python `urllib` 请求会被 B 站拦截返回 412，必须走系统 `curl`。`b23.tv` 短链接要先用 `curl -sIL` 解析出跳转后的真实网址
- **知乎**：无论是 `zhihu.com/question/...` 还是 `zhuanlan.zhihu.com/p/...`，页面里都嵌了一段包含 `"initialState"` 字符串的大 `<script>`，从里面正则匹配 `"created":\d+` / `"createdTime":\d+`，取最小值就是这条内容的真实发布时间戳（一个页面里经常会重复出现好几个时间戳，比如评论、相关推荐的时间，取最小值最可靠）
- **Medium**：页面里 `<script type="application/ld+json">` 直接有结构化的 `datePublished`/`dateCreated` 字段，比抓可见文字标题靠谱得多
- 知乎和 Medium 网站都会拦截 `WebFetch` 抓取（403），必须用浏览器工具（`mcp__Claude_Browser__javascript_tool` 在页面里跑上面那段正则）才能拿到内容
- 微博"当年发布只显示月-日、非当年才显示完整年份"这条规律（上一节已经记录过）这次继续复用，且 `m.weibo.cn/status/<id>` 优先于桌面版

这一轮也顺手确认了"资料收集表"工作流的一个正式约定：**表格里的"附注"列后来改名叫"添加文字"**，管理员每次把用户填过的 Excel 发回来之后，AI 要读这一列的内容并据此打标题/改分类，不确定的地方要问用户，而不是只处理"状态"列——这条已经是标准工作流的一部分，不是一次性动作。

同时给叶子内容 schema 新增了两种类型（`js/category.js` 的 `renderChildCard`、`js/kudos.js`、`js/search.js`、`css/style.css` 都相应改过，详见 README.md 第四节）：

- **`honor`**（荣誉/头衔）：`{"type":"honor","title":"...","year":2023}`，没有 `url`，渲染成不可点击的卡片（`.honor-card`），不参与点赞（`js/kudos.js` 的 `computeKudosTotal` 直接跳过），也不进搜索索引（`js/search.js` 的 `walk()` 不会递归进它，因为它没有可跳转的目的地）。用在"这是一项真实荣誉，但没有对应单一网页链接"的情况，比如奖项本身
- **`caption`**：不是独立的叶子类型，而是 `link` 节点和事件 `materials` 里每一项都可以加的可选字段，`js/common.js` 的 `captionHtml()` 把它渲染成一行引用样式的小字（`.material-caption`，斜体+左边一条紫色细线），显示在对应那条内容的正上方，用来标注"这条为什么特殊"之类的背景信息。这个功能是照着用户给的一张截图的视觉样式（一段引用文字）实现的

### 品牌活动/商务代言/杂志分类重组（2026-09-21~22）

这三块内容原本的位置：`杂志/时尚大片` 是"荣誉称号"下面的一个子分类；`历年商务代言`、`品牌活动` 也都在"荣誉称号"下面。用户分两步要求调整：

1. 先把 `杂志/时尚大片` 从"荣誉称号"里拿出来，单独列成首页的一个顶层大分类
2. 接着要求"历代商务代言，也放在品牌活动与杂志底下"——从这句话本身能看出用户心目中这个新顶层分类应该叫"品牌活动与杂志"，而不只是"杂志/时尚大片"，于是把 `历年商务代言` 和 `品牌活动` 也一起从"荣誉称号"移出来，三个一起组成了新的顶层 tab **品牌活动与杂志**（id `brand-magazines`，颜色 `#463A5C`），下面是三个并列的子分类：历年商务代言、品牌活动、杂志/时尚大片（原来的 2 条 CK 广告链接现在挂在"杂志/时尚大片"这个子分类下面，而不是直接平铺在顶层）

"荣誉称号"这个 tab 现在只剩：音乐、电影、话剧、其他荣誉 四个子分类。

### 分界线标签修正："其他" vs "年份不详"（2026-09-22）

用户发截图指出"百亿影帝 › 电影"这个分类页面里，"电影"和"电视剧"这两个子分类前面显示的分界线写着"年份不详"，但这俩明明是文件夹，不是"查不到年份的内容"。排查后确认是批量导入时脚本的一个系统性问题：给"按年份分组、超过一定数量就加分界线"这条规则写自动化脚本时，脚本把子文件夹也当成"没有 `date` 字段的普通内容"处理，跟真正查不到年份的链接混在了一起，一律标成"年份不详"。

排查方式：写了一个扫描脚本，遍历 `nav.json` 找"分界线后面紧跟着的一组内容里包含文件夹节点"的情况，全站找到了 **9 处**（百亿影帝、阿b小王子、dd、wb、昵称由来、考古汇总等）。处理规则：
- 如果分界线后面**全部是文件夹**，直接把标签从"年份不详"改成"其他"
- 如果分界线后面**文件夹和真正无年份的链接混在一起**（考古汇总这一处），拆成两组——无年份的链接留在原来的"年份不详"分界线下面，文件夹另起一条新的"其他"分界线放在后面

修完之后确立的约定（也写进了 README.md 第 4.2 节）：**"年份不详"只用于真正查不到发布年份的单条链接；子分类/文件夹如果需要跟按年份分组的内容分开，一律用"其他"**，因为文件夹不是"某一年发布的内容"，用"年份不详"是概念上的误用。

### 分类卡片预览逻辑两处修复（2026-09-22）

用户连续报告了两个"分类卡片下面第二行小字"的显示问题，两个都属于 `js/category.js` 里 `renderChildCard` 函数（README.md 5.10 节描述的那个规则）：

1. **预览文字混入了单条链接的标题**：比如"电影"这个文件夹下面同时有几条微博节庆活动链接（平铺的 `link`）和好几个电影名字的子分类（`功夫女足`/`不说话的爱`/`传说` 等），预览文字本该显示这几个电影名字，实际却显示成了"260717｜电影《十间敢死队》张艺兴CUT..."这种随机挑出来的某条微博标题——原因是原来的逻辑不管子节点是文件夹、事件还是链接，都会把它的标题塞进预览候选名单里。修法：预览候选名单直接跳过 `type === "link"` 的子节点，只保留文件夹标题和事件年份
2. **"N 项" 计数把分界线也算了进去**："向往的生活"这个文件夹实际是 14 条链接 + 2 条年份分界线，卡片却显示"16 项"。修法：算这个数字之前先把 `type === "divider"` 的节点过滤掉

两处都是当天在本地起测试服务器实际打开对应页面核实过效果之后才提交/推送的，改动都只有一行（分别是 `renderChildCard` 里 `names` 收集循环加一个 `type !== "link"` 的过滤条件、`count` 的计算加一个 `filter`），风险很低。

### 首页问卷征集链接（2026-09-21）

首页右上角新增了一个"补充链接"文字链接（白色字体，跟主标题"Archive of Lay"同样字号），点击跳转到用户提供的腾讯问卷 `https://wj.qq.com/s2/27980031/mk3h/`，用来收集粉丝想补充的资料/考古内容。纯前端一行 `<a>` 标签，不涉及任何后端或数据结构改动。

## 三、GitHub 仓库

- 地址：https://github.com/slu-x/zyx-archive（**2026-09-15 出于隐私考虑改过用户名**，原来叫 ShengyuanLu-x；GitHub 对旧地址还留着自动跳转，本地仓库 `origin` 已直接改成新地址）
- 注意：曾经短暂用过另一个账号 syluxback 建过同名空仓库，已废弃不用
- **提交身份**：这个仓库本地 git 配置（只作用于这一个仓库，没改全局设置）已经改成匿名身份（用户名 + GitHub 提供的 `users.noreply.github.com` 匿名邮箱），避免以后新提交暴露真实邮箱；但**改名之前的历史提交仍然带着用户真实邮箱**，用户已知情并选择保留现状（重写历史成本/风险较高，暂不做）
- 只有用户本人有推送权限

## 四、部署现状

先后部署了两套，目前香港 VPS 是主力测试地址，Cloudflare 保留做对照：

- **Cloudflare Workers**：https://zyx-archive.syluxback.workers.dev ，连接 GitHub 自动部署（push 到 main 自动重新发布），依赖仓库里的 wrangler.jsonc 和 .assetsignore 两个配置文件。**实测结论：中国大陆用户访问这个地址不理想**，这是转向香港 VPS 的直接原因
- **CMIVPS 香港 VPS（正式测试地址）**：https://archiveoflay.com （及 https://www.archiveoflay.com），服务器 IP 223.26.61.154，Ubuntu 24.04，套餐 HK.KVM.1C1G，$5/月，标注"China Friendly CN2 Network"。选它的原因：用户没有大陆身份证/护照无法完成腾讯云/阿里云国内站的强制实名认证，国际站又不保障对大陆的网络质量，CMIVPS 这类中小型服务商不需要实名且专门优化大陆连接。域名 `archiveoflay.com` 在 Namecheap 注册，DNS 用 Namecheap 自带的 BasicDNS，两条 A 记录（`@` 和 `www`）都指向服务器 IP。HTTPS 证书用 Let's Encrypt（certbot）签发，已设置系统定时任务自动续期，到期日 2026-11-29（会自动续，不用管）。服务器上部署了 Nginx 提供 /var/www/zyx-archive（GitHub 仓库的 clone），有一个每 5 分钟自动 git pull 的定时任务保持同步，防火墙只放行了 22/80/443。Nginx 还给 `.html`/`.js`/`.css` 这几类文件加了 `Cache-Control: no-cache`（2026-09-07，排查"电脑上显示的是改版前的旧样式，手机是对的"这个问题时加的——浏览器缓存了旧的代码文件，跟数据文件是两回事，`data/*.json` 那类请求早就在 `js/common.js` 里加了 `{cache:"no-cache"}`，但代码文件本身当时没处理）。**具体登录方式/密钥位置等运维细节问用户本人**（不写在这份公开文档里）

## 五、还没做完 / 需要用户确认的事

1. ~~"霸王别姬"事件的真实日期~~ 已确认：2019-12-31（2019年跨年舞台），事件文件已改名为 `new-year-2019-farewell-concubine.json`，并在 nav.json 里挂到"综合›舞台之神›跨年舞台"下面
2. ~~"B站舞台"和"观察日记"疑似同一个 BV 号重复~~ 已确认不是错误：是同一个多集视频的不同分集（`&p=` 参数区分），并且趁这次改版把"官方舞台"改成了正确的微博链接（原来误标成了跟"观察日记"一样的 B 站链接）
3. ~~README.md 需要更新~~ 已重写，现在完整覆盖了 nav.json 多层导航的维护说明（怎么加分类/链接/分界线/完整事件、怎么改配色等）
4. 已做了国内访问测试表（Claude Artifact），需要用户自己分享出去给粉丝测试，目前还没收到反馈
5. nav.json 经过 2026-09-21~22 这一轮大批量导入后，规模是 **642 条 link + 39 个 event + 44 条 honor + 139 个文件夹节点**，绝大部分历史占位分类已经填充了真实内容；个别分类可能还不完整或仍是空的，需要用户后续陆续补充——具体哪些空，建议直接读当前 `data/nav.json` 确认，不要依赖这份文档里某一次的旧快照数字（内容还在持续增长）
6. "贝壳英语角›而立24"下面 4 条 Medium 链接，因为 Medium 拦截了抓取标题的请求（403），暂时用"而立24（一）～（四）"占位命名，用户如果知道具体标题可以改得更准确
7. 视觉改版：深紫背景 + 浅紫卡片的多层导航已经做完并上线；用户提到以后还想给具体事件配专属背景图、首页加横幅——这两个还没做，等用户想清楚方案再做
8. 图片托管：纯链接数据规模再大也就几 MB 不用担心；如果以后上传真实图片文件，需要注意压缩尺寸、懒加载，目前还没有相关代码
9. ~~不建议做 Excel 自动导入工具~~ **2026-09-11 起已推翻**：现在用的就是"资料收集表 Excel + 腾讯文档"这一套轻量导入工作流（见上面对应章节），不要再套用旧结论
10. 网站访问量统计：用户问过能不能看访问量，讨论了三个方案还没定：(A) 直接读 Nginx 自带的 access log，AI 按需整理成报告；(B) 在同一台 VPS 上自装轻量开源统计工具（如 Umami），跟踪脚本从自己域名加载，不影响大陆访问速度，但会给服务器加一个"一直跑着的小程序"；(C) 用百度统计等国内统计服务。**明确不推荐 Google Analytics**（大陆访问不稳定，会拖累最在意的大陆访问体验）
11. HTTPS 已配置完成（archiveoflay.com，Let's Encrypt，自动续期，到期 2026-11-29），不再是待办事项
12. 5 个 2026-09-21 新分组的公益事件，文件名里还留着最初猜测的年份（比如 `gongyi-guangxi-flood-2025.json`，但内部真实 `date` 字段已经核实改成了 `2026-07-07`），是历史遗留的命名，不影响任何功能（`id` 只是不透明字符串，不参与排序/展示），只是以后如果有人对着文件名找事件可能会被年份误导。不算 bug，暂时不用主动处理，除非顺手整理文件名的时候一起改掉

## 六、和用户沟通时要注意的事

- 用户完全没有编程经验，技术概念第一次出现要用大白话解释
- 付款、注册账号、输入密码这类动作需要用户自己操作
- 用户很在意"不被单一平台绑定"，新增第三方依赖要说明以后怎么迁移
- AI 应该在有把握时直接给建议、指出用户设想里不合理的地方，不要每件事都只列选项
- 涉及"公开/分享"内容的操作需要用户自己执行
